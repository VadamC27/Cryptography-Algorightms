#ifndef HASH_TEST_H
#define HASH_TEST_H

#include "../cryptography_utils.h"
#include <ctime>
#include <stdlib.h>
#include <cstdio>

void hash_functions_test();

//unsigned char * hash_and_print(const char *algo_name, const unsigned char *input, size_t input_len); // msg always true
//unsigned char * hash_and_print(const char *algo_name, const unsigned char *input, size_t input_len, bool msg);

#endif /* HASH_TEST_H */